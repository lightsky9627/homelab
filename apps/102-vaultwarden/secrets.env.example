# 复制为 secrets.env。
# 这个文件用 env_file 加载，compose 不会做 ${} 插值，
# argon2 哈希里的 $ 可以原样写，不用转义（这点和 .env 不同）。

# ---- 管理面板访问令牌 ----
# 填了才能访问 /admin 管理面板（邀请用户、看配置、改 SMTP 等）。
# 留空则面板不可用，但保险库功能不受影响。
#
# 生成命令（交互式，会让你输入两遍密码）：
#   docker run --rm -it vaultwarden/server:latest /vaultwarden hash --preset owasp
#
# 把输出的完整哈希（$argon2id$...）原样粘到下面：
ADMIN_TOKEN=
