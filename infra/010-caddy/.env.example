# Caddy 专属配置（复制为 .env）
# 全局变量（TZ / ACME_EMAIL）在根目录 .env 里，这里不用重复写。

# ---- 镜像版本 ----
# Caddy 无状态，用 latest 配合 watchtower 自动更新很安全。
# 想锁定版本就改成具体的，例如 2.10.2-alpine
CADDY_TAG=latest

# ---- 代理（可选）----
# 国内服务器直连 ACME（Let's Encrypt / ZeroSSL）经常超时，
# 本机有代理的话填上，让 Caddy 签证书时走代理。
# 留空则直连。
#
# 格式：必须带协议，例如 http://127.0.0.1:7897
HTTP_PROXY=
HTTPS_PROXY=
# 不走代理的地址（逗号分隔）。Go 默认已对 localhost/127.0.0.1 豁免，
# 这里可以补内网网段，例如：
# NO_PROXY=localhost,127.0.0.1,192.168.0.0/16
NO_PROXY=
